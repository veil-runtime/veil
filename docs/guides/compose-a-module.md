---
title: Compose a module
---
# Compose a module

A module pairs a manifest and capabilities. Every capability supplied must be named in manifest.capabilities or runtime.use throws. Optional provider, permission, and minimum-operator manifest fields are not enforced in v0.2.0.
