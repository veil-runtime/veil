title: Veil
description: "Veil is a governed, capability-driven execution runtime. Reasoning produces plans; Veil validates, authorizes and executes registered operations."
url: "https://veil-runtime.github.io"
baseurl: "/veil"
exclude:
  - ARCHITECTURE.md
  - ROADMAP.md
  - index-old.md
  - adr
  - release
defaults:
  - scope:
      path: ""
    values:
      layout: docs
