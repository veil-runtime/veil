---
title: Testing
---
# Testing

Run `npm run typecheck` for TypeScript, `npm test` for compiled Node tests, and `npm run check` before handoff. Tests are authoritative for execution ordering, references, authorization, and planner routing.
